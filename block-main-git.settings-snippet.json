{
  "_comment": "Merge this into your repo's .claude/settings.json to activate the branch-guard hook. It runs before every Bash call and denies git commit/push while on main.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -File .claude/hooks/block-main-git.ps1"
          }
        ]
      }
    ]
  }
}
