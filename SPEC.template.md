# Spec — <FEATURE_NAME>

> **How to use this template:** Copy to `SPEC.md` (or `docs/specs/<feature>.md`) when starting
> a feature. Fill it in and agree on it **before** writing code — this is the thing to review,
> not the implementation. Keep it updated as decisions change; it's the source of truth.
> For a one-line edit, skip this; for anything bigger, it pays for itself.

## Outcome
What should be true when this is done, in plain language. The user-visible result.

## In scope
- <bullet list of what this change WILL do>

## Out of scope
- <what this change will NOT do — name it explicitly so the agent doesn't drift into it>

## Constraints
- <hard limits: deps allowed/forbidden, performance, "no new outbound calls", style, etc.>

## Prior decisions to respect
- <anything already settled that the implementation must not re-litigate>

## Plan (files & steps)
- `<path>` — <what changes>
- `<path>` — <what changes>

## How we'll verify it works
- <the exact test / command / screenshot that proves success — pass/fail, not "looks done">

## Open questions
- <anything still undecided; resolve before or during, and record the answer here>
