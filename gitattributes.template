# Normalize text files; let Git decide line endings per platform.
* text=auto

# Keep shell scripts LF-only so they run under Git's bundled sh on Windows.
# Without this, core.autocrlf converts hook scripts to CRLF on checkout and sh fails
# with "syntax error: unexpected end of file". This rule is last so it wins for *.sh.
*.sh text eol=lf
